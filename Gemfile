source "https://rubygems.org"

# Use the same Jekyll version GitHub Pages uses, so local builds match production.
gem "github-pages", group: :jekyll_plugins

# Windows / Ruby 3.x: run `bundle install`, then `bundle exec jekyll serve`
# (requires Ruby: https://rubyinstaller.org on Windows)
